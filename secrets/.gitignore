# Only allow encrypted .age files and secrets.nix to be committed
# NEVER commit decrypted secrets or keys

# Deny everything by default
*

# Allow encrypted age files
!*.age

# Allow the secrets configuration
!secrets.nix

# Allow this gitignore file itself
!.gitignore
